\documentclass{standalone}
\usepackage{tikz}
\usetikzlibrary{graphs} % load the graphs library

\begin{document}
% --- Undirected triangle graph ---
\tikz \graph[nodes={draw,circle}] { A -- B -- C -- A };

% --- Directed triangle graph ---
\tikz \graph[nodes={draw,circle}] { A -> B -> C -> A };
\end{document}
