\graph[nodes={draw,circle}] { A -> B -> C -> D };

% Path P4
\graph[nodes={draw,circle}] { subgraph I_n [n=4] };

% Cycle C5
\graph[nodes={draw,circle}, circular placement, radius=1cm] { subgraph C_n [n=5] };
\end{tikzpicture}

\end{document}
