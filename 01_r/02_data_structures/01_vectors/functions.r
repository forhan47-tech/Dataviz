# Vector Functions
vec <- c(10, 20, 30, 40)

print(length(vec))   # number of elements
print(sum(vec))      # sum
print(mean(vec))     # average
print(sort(vec))     # sorted
print(unique(c(1, 2, 2, 3, 3, 4))) # distinct values