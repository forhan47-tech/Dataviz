# Create a factor
ft <- factor(c("apple", "banana", "apple", "orange", "banana", "apple"))

# Analyze the factor
levels(ft)  # Levels of the factor
summary(ft) # Summary of the factor
table(ft)   # Frequency table of the factor