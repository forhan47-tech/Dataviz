# Basic R Script: Vectors
vec <- c(1, 2, 3, 4, 5)

# Analyze the vector
length(vec)     # Number of elements
range(vec)      # Minimum and maximum as a vector
table(vec)      # Returns: Frequency count of each value